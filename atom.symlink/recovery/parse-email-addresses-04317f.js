import { moduleFor, test } from 'ember-qunit';

moduleFor('utils:parse-email-addresses', 'Unit | Utils | parse email addresses', {
  // Specify the other units that are required for this test.
  // needs: ['service:foo']
});

// Replace this with your real tests.
test('it parses emails correctly from arbitrary text', function(assert) {
  assert.ok(service);
});
